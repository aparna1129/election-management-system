{
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n CLIENT,SERVER -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}